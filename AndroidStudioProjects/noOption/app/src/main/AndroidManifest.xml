<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--  REQUIRED FOR GEMINI / INTERNET CALLS -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--  REQUIRED FOR SCREEN TIME / USAGE ACCESS -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!--  Package visibility (Android 11+) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoOption">

        <activity
            android:name=".WebsiteTimerActivity"
            android:exported="false" />

        <activity
            android:name=".PermanentWebsiteSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".PermanentAppsViewActivity"
            android:exported="false" />

        <activity
            android:name=".FocusTimerActivity"
            android:exported="false" />

        <activity
            android:name=".PermanentSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".AppSelectionActivity"
            android:exported="false" />

        <activity
            android:name=".PermissionSetupActivity"
            android:excludeFromRecents="true"
            android:exported="false" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!--  Blocking screen -->
        <activity
            android:name=".BlockActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:taskAffinity="" />

        <!--  Dashboard -->
        <activity
            android:name=".DashboardActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleTask"
            android:taskAffinity="" />

        <!--  APP LAUNCHER -->
        <activity
            android:name=".MainActivity"
            android:excludeFromRecents="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--  Accessibility Service -->
        <service
            android:name=".MyAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>

        <receiver
            android:name=".MyDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="false">

            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_config" />

            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>


    </application>
</manifest>
